#include<iostream>
#include<math.h>
#include<string>
#include<algorithm>
using namespace std;
int main(){
// to print n 5 times  \ string s(5,'h');
// cin>>s; // to take input   
// to take spaces between words/   getline(cin,s);
string s1="harsh ",s2="malik ";
// to add s2 in s1 ,/   use s1+s2 or s1.append(s2);
// to access character /  s[i]
// to clear strring  /   s1.clear();
// s1.compare(s2)  0- for equal ,  random number for not equal  
// to check whether a string is empty 
// s1.clear();
// if(s1.empty())
// to erase something from index 3 to (1 word) from it 
//   s1.erase(3,1);
// to find /  s1.find("ha")
// to insert something /  s1.insert(6,"malik");
// to calculate size /  cout<<s1.size();
// to make substring  / string s=s1.substr(2,3);
// string s="786";
// convert string to integer  / int x=stoi(s);
// int x=7543;
//  to convert integer to string / cout<<to_string(x);    
// to sort thestring /  sort(s1.begin(),s1.end());
// for upper case /transform(s1.begin(),s1.end(),s1.begin(),::toupper);
cout<<s1;
}